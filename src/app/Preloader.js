import { BounceLoader, HashLoader } from "react-spinners";

export default function Preloader(){
    return(
        // <HashLoader speedMultiplier={2} color={'#66bfff'}/>
        <BounceLoader speedMultiplier={2} color={'#66bfff'}/>

    )
}